spring:
  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.H2Dialect
logging:
  level:
    com:
      yulikexuan:
        sfg:
          ssmlab: debug